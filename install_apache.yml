---

- hosts: all
  become: true
  tasks:

  - name: reboot
    command:
      "shutdown -r now"

  - name: install apache2 package
    apt:
      name: apache2
      state: absent

  - name: add php support for apache
    apt:
     name: libapache2-mod-php
     state: absent
